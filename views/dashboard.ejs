<!DOCTYPE html>
<html lang="pl" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <title>CTF Shop Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { background-color: #1a1a1a; padding-top: 70px; }
        .navbar { background-color: #333; }
        .card { background-color: #2c2c2c; border: none; }
        .table-dark { background-color: #2c2c2c; }
        .secret-alice-box { border: 2px dashed #0f0; background: rgba(0, 255, 0, 0.1); }
    </style>
</head>
<body>

    <nav class="navbar navbar-expand-md navbar-dark fixed-top px-4">
        <a class="navbar-brand" href="#"><i class="fa-solid fa-basket-shopping"></i> CTF Shop</a>
        <div class="collapse navbar-collapse justify-content-end">
            <ul class="navbar-nav">
                <li class="nav-item d-flex align-items-center me-3">
                    <span class="text-white me-2"><%= user.username %></span>
                    <% if(user.role === 'admin') { %> <span class="badge bg-danger">ADMIN</span> <% } else { %> <span class="badge bg-success">USER</span> <% } %>
                </li>
                <li class="nav-item"><a class="btn btn-outline-light btn-sm" href="/">Wyloguj</a></li>
            </ul>
        </div>
    </nav>

    <div class="container mt-4">
        
        <% if(user.username === 'alice@test.com' && user.role === 'admin') { %>
        <div class="alert secret-alice-box text-white mb-5">
            <div class="d-flex align-items-center">
                <div class="display-4 me-4"><i class="fa-solid fa-user-secret text-success"></i></div>
                <div>
                    <h4 class="alert-heading text-success">Gratulacje Alice!</h4>
                    <p>Udało Ci się przeprowadzić atak <strong>Privilege Escalation</strong>.</p>
                    <p class="mb-0">Jako Alice posiadasz teraz uprawnienia Administratora. Twój tajny kod to: <code>FLAG{ALICE_IS_THE_BOSS_NOW}</code></p>
                </div>
            </div>
        </div>
        <% } %>

        <div class="row mb-5">
            <div class="col-md-8">
                <h4 class="text-warning mb-3">Twoje Faktury</h4>
                <table class="table table-dark table-hover">
                    <thead>
                        <tr><th>#ID</th><th>Tytuł</th><th>Akcja</th></tr>
                    </thead>
                    <tbody>
                        <% invoices.forEach(inv => { %>
                        <% if (inv.id != 11 && inv.id != 101) { %>
                        <tr>
                            <td><%= inv.id %></td>
                            <td><%= inv.title %></td>
                            <td>
                                <a href="/invoice/<%= inv.id %>" target="_blank" class="btn btn-sm btn-warning">
                                    <i class="fa-solid fa-file-arrow-down"></i> Otwórz
                                </a>
                            </td>
                        </tr>
                        <% } %>
                        <% }) %>
                    </tbody>
                </table>
            </div>

            <div class="col-md-4">
                <div class="card">
                    <div class="card-header bg-primary text-white">Twój Profil</div>
                    <div class="card-body">
                        <ul class="list-unstyled">
                            <li><strong>Imię:</strong> <%= user.firstName %></li>
                            <li><strong>Nazwisko:</strong> <%= user.lastName %></li>
                            <li><strong>Adres:</strong> <%= user.address %></li>
                        </ul>
                        <a href="/user/edit?id=<%= user.id %>" class="btn btn-outline-light w-100 mt-2">
                            <i class="fa-solid fa-pen-to-square"></i> Edytuj Dane
                        </a>
                        
                        <button type="button" class="btn btn-danger w-100 mt-2" data-bs-toggle="modal" data-bs-target="#passwordModal">
                            <i class="fa-solid fa-key"></i> Zmień Moje Hasło
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <% if(user.role === 'admin') { %>
        <hr class="border-danger my-5">
        <h3 class="text-danger">PANEL ADMINA</h3>
        
        <div class="card border-warning mt-3">
            <div class="card-header bg-warning text-dark">Wszystkie Faktury (Systemowe)</div>
            <div class="card-body p-0" style="max-height: 300px; overflow-y: auto;">
                <table class="table table-dark table-sm mb-0">
                    <thead><tr><th>ID</th><th>Właściciel</th><th>Tytuł</th><th>Akcja</th></tr></thead>
                    <tbody>
                        <% allInvoices.forEach(inv => { %>
                        
                        <% if (inv.id != 11 && inv.id != 101) { %>
                        <tr>
                            <td><%= inv.id %></td>
                            <td>User #<%= inv.owner_id %></td>
                            <td class="text-muted"><%= inv.title %></td>
                            <td>
                                <div class="btn-group">
                                    <a href="/invoice/<%= inv.id %>" target="_blank" class="btn btn-sm btn-outline-light py-0" style="font-size: 0.7rem;">Otwórz</a>
                                    <form action="/admin/invoice/delete" method="POST" class="d-inline ms-1">
                                        <input type="hidden" name="invoice_id" value="<%= inv.id %>">
                                        <button class="btn btn-sm btn-danger py-0" style="font-size: 0.7rem;">DEL</button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                        <% } %>
                        
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>
        
        <div class="card border-danger mt-4">
             <div class="card-header bg-danger text-white">Baza Użytkowników</div>
             <div class="card-body p-0">
                 <table class="table table-dark table-striped table-hover mb-0">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Login</th>
                            <th>Imię i Nazwisko</th>
                            <th>Rola</th>
                            <th class="text-end">Akcje</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% allUsers.forEach(u => { %>
                        <tr>
                            <td><%= u.id %></td>
                            <td><%= u.username %></td>
                            <td><%= u.firstName %> <%= u.lastName %></td>
                            <td>
                                <% if(u.role === 'admin') { %>
                                    <span class="badge bg-danger">ADMIN</span>
                                <% } else { %>
                                    <span class="badge bg-secondary">USER</span>
                                <% } %>
                            </td>
                            <td class="text-end">
                                <a href="/user/edit?id=<%= u.id %>" class="btn btn-sm btn-outline-warning py-0 me-1">Edytuj</a>
                                
                                <% if(u.username !== 'admin') { %>
                                    <form action="/admin/user/delete" method="POST" class="d-inline">
                                        <input type="hidden" name="target_id" value="<%= u.id %>">
                                        <button class="btn btn-sm btn-outline-danger py-0">Usuń</button>
                                    </form>
                                <% } else { %>
                                    <button class="btn btn-sm btn-secondary py-0" disabled>Usuń</button>
                                <% } %>
                            </td>
                        </tr>
                        <% }) %>
                    </tbody>
                 </table>
             </div>
        </div>

        <div class="text-center mt-4 mb-5">
            <a href="/debug/dump" class="text-muted small">[Debug API]</a>
        </div>
        <% } %>
    </div>

    <div class="modal fade" id="passwordModal" tabindex="-1" aria-labelledby="passwordModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content bg-dark text-white border-secondary">
                <div class="modal-header border-secondary">
                    <h5 class="modal-title" id="passwordModalLabel">Zmiana Hasła</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <form action="/user/update" method="POST">
                        <input type="hidden" name="id" value="<%= user.id %>">
                        
                        <div class="mb-3">
                            <label for="newPass" class="form-label">Nowe Hasło</label>
                            <input type="password" class="form-control bg-secondary text-white border-0" id="newPass" name="password" required>
                        </div>
                        <div class="d-flex justify-content-end">
                            <button type="button" class="btn btn-secondary me-2" data-bs-dismiss="modal">Anuluj</button>
                            <button type="submit" class="btn btn-danger">Zatwierdź Zmianę</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>